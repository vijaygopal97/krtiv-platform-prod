/** Set to `'expanded'` to restore the previous large card layout. */
export type DestinationFactsLayout = 'compact' | 'expanded';

export const DESTINATION_FACTS_LAYOUT: DestinationFactsLayout = 'compact';
