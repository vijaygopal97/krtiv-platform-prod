/**
 * Temporary UI toggles — set to `true` to restore features.
 */
export const FEATURE_SHEKRU_MASCOT = false;

/** Top navigation only (/about and /contest-registration routes still work). */
export const FEATURE_HEADER_ABOUT_LINK = false;

export const FEATURE_HEADER_CONTEST_LINK = false;
